{
  "name": "skillforge",
  "version": "0.1.0",
  "module": "src/cli/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run src/cli/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "commander": "^12.0.0",
    "zod": "^3.23.0",
    "execa": "^9.0.0",
    "fast-glob": "^3.3.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
