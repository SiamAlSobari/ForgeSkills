# Plan: SkillForge — Phase 0: Project Setup

## Goal
Setup project SkillForge dari nol sesuai PRD §21 dan todo.md Phase 0.

## Current State
- Git repo sudah ada (branch `main`)
- Hanya ada `prd.md` dan `todo.md`
- Belum ada folder structure, package.json, atau config

## Steps

### 1. Create Folder Structure (PRD §21)
Buat semua direktori sesuai struktur repository:
```
skillforge/
├── shared/
│   ├── analyzer/
│   ├── findings/
│   └── report/
├── skills/
│   ├── security-audit/
│   ├── bug-investigate/
│   ├── performance-audit/
│   ├── architecture-review/
│   ├── dependency-review/
│   ├── database-review/
│   └── release-check/
├── src/
│   ├── cli/
│   └── config/
├── tests/
├── docs/
├── examples/
├── templates/
```

### 2. Init Bun Project
- `bun init` di root
- Set name: `skillforge`
- Set entrypoint: `src/cli/index.ts`

### 3. Install Dependencies
Production:
- `commander` — CLI framework
- `zod` — validation
- `execa` — process execution
- `fast-glob` — file scanning

Dev:
- `vitest` — testing
- `typescript` — type safety
- `@types/bun` — Bun types

### 4. Setup Config Files
- `tsconfig.json` — strict mode, ESNext target
- `vitest.config.ts` — basic config
- `.gitignore` — node_modules, dist, .env, etc.

### 5. Setup GitHub Actions CI
- `.github/workflows/ci.yml`
- Jobs: lint + test
- Trigger: push & PR to main

### 6. Mark Todo Items
Update `todo.md` — centang semua item Phase 0 yang sudah selesai.

## Files to Create/Modify
- `package.json` (via bun init)
- `tsconfig.json`
- `vitest.config.ts`
- `.gitignore`
- `.github/workflows/ci.yml`
- `todo.md` (mark Phase 0 complete)
- Semua folder structure (empty dirs with .gitkeep)

## Verification
- `bun install` berhasil
- `bun run tsc --noEmit` tidak error
- `bun test` bisa jalan (walau kosong)
- Folder structure sesuai PRD §21
